# Environment template

The frontend-only site does not require runtime environment variables. Keep this file as a reminder that no database, OAuth, Supabase, or server secrets are needed for the public build.

Do not add passwords or private source URLs to the repository. Reader letters are local browser data, and the curator puzzle is an editorial convenience rather than server-grade security.
